{
  "extends": "@vue/tsconfig/tsconfig.node.json",
  "include": ["vite.config.*", "vitest.config.*", "cypress.config.*", "playwright.config.*"],
  "compilerOptions": {
    "composite": true, // 合成的意思 就是config.json可以把这个文件并入
    "types": ["node"] // include 可能用到node环境
  }
}
